// Dinner controller that we use whenever we have view that needs to
// display or modify the dinner menu
pokeBattleApp.controller('ListCtrl', function ($scope,Battle) {

    // $scope.selectedMenu = Dinner.getSelectedMenu();

});
